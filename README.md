# hoshiorange-official

my introduce.

## 開発手順

```bash
# 1. 依存関係をインストール
npm install

# 2. 環境変数を用意
cp .env.local.example .env.local
# .env.local を編集（YOUTUBE_API_KEY / YOUTUBE_CHANNEL_ID / NEXT_PUBLIC_X_USERNAME / NEXT_PUBLIC_SITE_URL）

# 3. 開発サーバー起動
npm run dev
```

`http://localhost:3000` で確認。
